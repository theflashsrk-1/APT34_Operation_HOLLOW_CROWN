# Push to GitHub

```bash
cd Red-Range4
git init
git add .
git commit -m "APT34 Operation HOLLOW CROWN — AD LDAP & Service Exploitation Range"
git branch -M main
git remote add origin git@github.com:hacktifytechnologies/Red-Range4.git
git push -u origin main
```
